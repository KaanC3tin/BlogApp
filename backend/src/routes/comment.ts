import express from "express";
const router = express.Router();
// import { getBlogs } from "../controllers/"


router.delete("/:commentId",);
router.get("/:blogId",); //  /api/comments
router.post("/",);




export default router;